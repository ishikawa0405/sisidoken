using UnityEngine;
using System.Collections;

public class NewBehaviourScript1 : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Create(gameObject, 3.0f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
